# Fleet registry

This directory holds the **fleet map** consumed by
`scripts/build_global_manifest.py` and surfaced through
`global_manifest.json → fleet_map`.

There are two inputs, in priority order:

1. **`fleet_registry.json`** — operator-curated, authoritative.
   Hand-edited and committed. This is the single source of truth.
2. **`fleet_discovery.json`** — optional, machine-generated by
   `scripts/total_fleet_crawler.py`. Only entries whose `repo_name`
   is *not* already present in the registry are appended (with
   `source: "discovery"`). The crawler never overwrites registry
   entries and never invents Hugging Face URLs.

Both files share the same entry schema:

```json
{
  "schema_version": "1.0.0",
  "entries": [
    {
      "repo_name": "Vortex",
      "github_url": "https://github.com/DJ-Goana-Coding/Vortex",
      "hf_space_url": "https://huggingface.co/spaces/DJ-Goana-Coding/Vortex",
      "role": "Vortex",
      "status": "active"
    }
  ]
}
```

Field rules:

* `repo_name` (required, string) — unique key.
* `github_url` (required, string) — canonical repo URL.
* `hf_space_url` (optional, string or `null`) — only set when known.
  **Never** invented to "look complete".
* `role` (optional, string) — e.g. `Vortex`, `Pioneer`, `Omega`,
  `Nexus`. Free-form label.
* `status` (optional, string) — e.g. `active`, `archived`, `unknown`.

Entries are sorted by `repo_name` in the emitted `fleet_map` for
deterministic output. If both files are absent or empty, the manifest
emits `"fleet_map": {"entries": []}` rather than crashing.
